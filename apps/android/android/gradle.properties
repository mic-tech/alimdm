# Project-wide Gradle settings.
android.useAndroidX=true
android.enableJetifier=true
# Increased heap for RN build
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -XX:+HeapDumpOnOutOfMemoryError

# Release signing credentials do NOT belong here — this file is tracked.
# Put them in android/keystore.properties (gitignored); see android/keystore.properties.example.
